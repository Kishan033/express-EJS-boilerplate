# Boilerplate for basic express application.

* Clone the repository.
* Create `.env`.
* `npm i`.
* `npm start`.
* Application should start on port `3000`.

# Publish
* docker build -t "my-node-app" .
 
* docker-compose up

